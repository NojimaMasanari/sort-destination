アプリケーション使用方法

1.ターミナルよりrun.pyを実行。
2.画面が表示される。検索ボックスに「検索したい場所」「検索したいカテゴリ」を書き込む。
　セレクトボックスから優先したいカテゴリを選択。
3.すべて選択したら「セット」ボタンを押し画面が切り替わるまで待機。
4.画面が切り替わると情報がセットされた状態なので「実行」ボタンを押す。
5.GoogleMaps上に検索結果の座標がマーカーとして表示される。
　テーブルが優先度順に作成される。